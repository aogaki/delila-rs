/target
tools/*/target/

.serena
.claude
.mcp.json

# Data output files
/data/
docker/data/
docker/.env
legacy

# Logs
/logs/

# IDE settings
.vscode/

# OS files
.DS_Store

# Hardware dumps
devtree.json

# CAEN GPL-licensed headers — NOT vendored, resolved from the system install
# (/usr/local/include) at build time so this repo can stay permissively licensed.
# Install the CAEN Digitizer Library to build with `--features x743`.
src/reader/caen_legacy/CAENDigitizer.h
src/reader/caen_legacy/CAENDigitizerType.h

# Third-party CAEN copyrighted material — not redistributable, kept local only.
# Obtain these from CAEN (www.caen.it) directly.
docs/Setting_CHARGE_MODE_for_the_x743_modules.pdf
tools/hv_calibration/caengeco2020.png
external/27xx-digitizer-usb-driver/

# CAEN library archives (proprietary — obtain from CAEN, never commit)
external/*.tar
external/*.tgz
external/*.tar.gz
external/*.gz
external/CAEN_Toolbox*

# Upstream A3818 driver source (unpatched). Patched version is the
# caen-a3818-driver submodule.
external/a3818_linux_driver-v1.6.12/

# AMax
TrapezoidalfilterMCA_dpp_tests
AMAX_firmware32_channel_4input_caenlist

# Compiled binaries
tools/delila2root/delila2root
tools/delila2root/delila2root_*
tools/root_sink/root_sink
__pycache__/

# CAEN library runtime logs. libCAEN* drops these into whatever directory the
# process was started from as soon as a board is opened, so they appear in the
# repo root on any DAQ host (CAENBoard_<model>Log.txt, CAENParametersLog.txt).
CAEN*Log.txt

# Test output files
*.root

# Throughput / stress sweep CSV output
/throughput_*.csv
/stress*.csv

tmp
memo*
shift_manual
2026_pid
results
FW


# Per-host MongoDB override (secrets / remote host) — never committed
scripts/mongodb.local.env

# Affinity lock files
*~lock~
